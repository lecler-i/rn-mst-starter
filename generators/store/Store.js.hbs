import {types} from 'mobx-state-tree';
import remotedev from 'mobx-remotedev';

const {{properCase name}}Store = types.model('{{properCase name}}Store', {
  name: types.string,
}).views(self => {
  return {};
}).actions(self => {
  return {};
});

const instance = {{properCase name}}Store.create({
  name: 'Test',
});

export default remotedev(instance);
